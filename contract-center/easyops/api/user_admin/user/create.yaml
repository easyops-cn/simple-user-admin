_version_: 2.0
_kind_: "interface"

version: 1.0.0
name: "Create"
description: "创建用户"

import:
  - easyops/model/user/user

endpoint:
  method: "POST"
  uri: "/api/v1/users"

request:
  type: User
  required:
    - User.*

response:
  type: User
  required:
    - User.*
